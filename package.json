{
  "name": "gallery",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "ciasteczko": "echo Sernik",
    "start": "http-server app/",
    "deploy": "gh-pages -d app/"
  },
  "devDependencies": {
    "gh-pages": "^2.1.1",
    "http-server": "^0.12.0"
  }
}
